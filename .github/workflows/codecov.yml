name: Workflow for Codecov (with Cython coverage)

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  run:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov coverage setuptools cython

      - name: Clean build
        run: |
          python setup.py clean --all
          rm -rf build *.so *.pyd *.c

      - name: Build Cython extensions
        run: |
          # 开启 Cython linetrace
          export CFLAGS="-O0 -DCYTHON_TRACE=1"
          python setup.py build_ext --inplace

      - name: Install package
        run: |
          pip install -e .

      - name: Run tests and generate coverage
        run: |
          python -m coverage run --source=classix -m pytest unittests.py
          python -m coverage xml -o coverage.xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: coverage.xml
          fail_ci_if_error: true
          verbose: true